# Uncomment the next line to define a global platform for your project
platform :macos, '10.15'

target 'MusiControl' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MusiControl
  pod 'BlueSocket'
  pod 'Kingfisher', '~> 5.0'
end

target 'MusiControlBridge' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for MusiControlBridge
  pod 'BlueSocket'
end
